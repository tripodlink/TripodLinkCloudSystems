@{
    ViewData["Title"] = "Home Page";
}

    <div class="text-center">
        <h1 class="display-4">Welcome to Landing Page</h1>
        <a href="@Url.Action("index", "register-donor", new { Area = "bb", Folder = "donor-management"})" class="btn btn-primary">Donor Management</a>
        <a href="@Url.Action("logout", "home", new { Area = "home", Folder = "home"})" class="btn btn-warning">Sign-Out</a>
    </div>
